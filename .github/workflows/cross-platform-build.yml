name: Build Multi-Platform

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            os_name: macOS
            rid: osx-x64
            output_name: 16CoreCharacterMapper-macOS-x64
          - os: ubuntu-latest
            os_name: Linux
            rid: linux-x64
            output_name: 16CoreCharacterMapper-Linux-x64

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    # NEW STEP: Extract Version from .csproj
    - name: Get Version
      id: get_version
      shell: bash
      run: |
        # Extract content between <Version> tags
        VERSION=$(sed -n 's/.*<Version>\(.*\)<\/Version>.*/\1/p' SixteenCoreCharacterMapper.Avalonia/SixteenCoreCharacterMapper.Avalonia.csproj | tr -d '\r' | xargs)
        echo "Detected App Version: $VERSION"
        echo "APP_VERSION=$VERSION" >> $GITHUB_ENV

    - name: Publish
      run: |
        dotnet publish SixteenCoreCharacterMapper.Avalonia/SixteenCoreCharacterMapper.Avalonia.csproj \
        -c Release \
        -r ${{ matrix.rid }} \
        --self-contained true \
        -p:PublishSingleFile=true \
        -p:IncludeNativeLibrariesForSelfExtract=true \
        -o publish_output

    # --- macOS Specific Steps ---
    - name: Generate Icons (macOS)
      if: matrix.os == 'macos-latest'
      run: |
        generate_icns() {
            local src="$1"
            local name="$2"
            local iconset="${name}.iconset"
            
            mkdir "$iconset"
            sips -z 16 16     "$src" --out "${iconset}/icon_16x16.png"
            sips -z 32 32     "$src" --out "${iconset}/icon_16x16@2x.png"
            sips -z 32 32     "$src" --out "${iconset}/icon_32x32.png"
            sips -z 64 64     "$src" --out "${iconset}/icon_32x32@2x.png"
            sips -z 128 128   "$src" --out "${iconset}/icon_128x128.png"
            sips -z 256 256   "$src" --out "${iconset}/icon_128x128@2x.png"
            sips -z 256 256   "$src" --out "${iconset}/icon_256x256.png"
            sips -z 512 512   "$src" --out "${iconset}/icon_256x256@2x.png"
            sips -z 512 512   "$src" --out "${iconset}/icon_512x512.png"
            sips -z 1024 1024 "$src" --out "${iconset}/icon_512x512@2x.png"
            
            iconutil -c icns "$iconset" -o "${name}.icns"
            rm -rf "$iconset"
        }

        if [ -f "SixteenCoreCharacterMapper.Avalonia/Assets/16Core Logo v3 Retina.png" ]; then
            generate_icns "SixteenCoreCharacterMapper.Avalonia/Assets/16Core Logo v3 Retina.png" "AppIcon"
        fi

        if [ -f "SixteenCoreCharacterMapper.Avalonia/Assets/16Core File Icon v1 Retina.png" ]; then
            generate_icns "SixteenCoreCharacterMapper.Avalonia/Assets/16Core File Icon v1 Retina.png" "FileIcon"
        fi

    - name: Create macOS App Bundle
      if: matrix.os == 'macos-latest'
      run: |
        APP_NAME="16Core Character Mapper"
        EXECUTABLE_NAME="SixteenCoreCharacterMapper.Avalonia"
        
        mkdir -p "$APP_NAME.app/Contents/MacOS"
        mkdir -p "$APP_NAME.app/Contents/Resources"
        
        cp -a publish_output/. "$APP_NAME.app/Contents/MacOS/"
        
        if [ -f "AppIcon.icns" ]; then cp "AppIcon.icns" "$APP_NAME.app/Contents/Resources/"; fi
        if [ -f "FileIcon.icns" ]; then cp "FileIcon.icns" "$APP_NAME.app/Contents/Resources/"; fi
        
        # Info.plist now uses ${{ env.APP_VERSION }}
        cat > "$APP_NAME.app/Contents/Info.plist" <<EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleExecutable</key>
            <string>$EXECUTABLE_NAME</string>
            <key>CFBundleIconFile</key>
            <string>AppIcon.icns</string>
            <key>CFBundleIdentifier</key>
            <string>com.georgetsirogiannis.16core</string>
            <key>CFBundleName</key>
            <string>$APP_NAME</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleShortVersionString</key>
            <string>${{ env.APP_VERSION }}</string>
            <key>LSMinimumSystemVersion</key>
            <string>10.15</string>
            <key>NSHighResolutionCapable</key>
            <true/>
            <key>CFBundleDocumentTypes</key>
            <array>
                <dict>
                    <key>CFBundleTypeExtensions</key>
                    <array>
                        <string>16core</string>
                    </array>
                    <key>CFBundleTypeIconFile</key>
                    <string>FileIcon.icns</string>
                    <key>CFBundleTypeName</key>
                    <string>16Core Project</string>
                    <key>CFBundleTypeRole</key>
                    <string>Editor</string>
                    <key>LSHandlerRank</key>
                    <string>Owner</string>
                </dict>
            </array>
        </dict>
        </plist>
        EOF

    - name: Create DMG (macOS)
      if: matrix.os == 'macos-latest'
      # Output filename now includes version
      run: |
        hdiutil create -volname "16Core Character Mapper" -srcfolder "16Core Character Mapper.app" -ov -format UDZO "${{ matrix.output_name }}_v${{ env.APP_VERSION }}.dmg"

    # --- Linux Specific Steps ---
    - name: Create Linux Archive
      if: matrix.os == 'ubuntu-latest'
      # Output filename now includes version
      run: |
        cd publish_output
        tar -czvf ../${{ matrix.output_name }}_v${{ env.APP_VERSION }}.tar.gz *

    # --- Upload Artifacts ---
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.output_name }}_v${{ env.APP_VERSION }}
        path: |
          *_v${{ env.APP_VERSION }}.dmg
          *_v${{ env.APP_VERSION }}.tar.gz

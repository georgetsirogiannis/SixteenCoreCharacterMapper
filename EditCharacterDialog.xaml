<Window x:Class="SixteenCoreCharacterMapper.EditCharacterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SixteenCoreCharacterMapper"
        Title="Edit Character" Height="Auto" Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Window.Resources>
        <local:ColorToBooleanConverter x:Key="ColorToBoolConverter"/>
    </Window.Resources>
    <StackPanel Margin="10" VerticalAlignment="Center">

        <TextBlock x:Name="NameLabel" Text="Name"/>
        <TextBox x:Name="NameBox" Margin="0,5,0,15"/>

        <TextBlock x:Name="ColorLabel" Text="Color"/>
        <TabControl x:Name="ColorTabControl" Margin="0,5,0,0">
            <TabItem x:Name="PaletteTab" Header="Palette">
                <ItemsControl x:Name="PresetColorsList" Margin="5">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton GroupName="PresetColor" Width="22" Height="22" Margin="2"
                                         ToolTip="{Binding Name}"
                                         Click="PresetColor_Click">
                                <RadioButton.Style>
                                    <Style TargetType="RadioButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="RadioButton">
                                                    <Grid>
                                                        <Ellipse x:Name="Highlight" Stroke="DodgerBlue" StrokeThickness="2" Visibility="Collapsed"/>
                                                        <Ellipse Width="18" Height="18">
                                                            <Ellipse.Fill>
                                                                <SolidColorBrush Color="{Binding Color}" />
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Highlight" Property="Visibility" Value="Visible"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </RadioButton.Style>
                                <RadioButton.IsChecked>
                                    <MultiBinding Converter="{StaticResource ColorToBoolConverter}" Mode="OneWay">
                                        <Binding Path="Color"/>
                                        <Binding ElementName="ColorPickerControl" Path="SelectedColor"/>
                                    </MultiBinding>
                                </RadioButton.IsChecked>
                            </RadioButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>
            <TabItem x:Name="CustomTab" Header="Custom">
                <local:SimpleColorPicker x:Name="ColorPickerControl" Margin="5"/>
            </TabItem>
        </TabControl>

        <TextBlock x:Name="CharacterTypeLabel" Text="Character Type" Margin="0,15,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
            <RadioButton x:Name="MainRadioButton" Content="Main" GroupName="CharacterType" Margin="10,0"/>
            <RadioButton x:Name="SupportingRadioButton" Content="Supporting" GroupName="CharacterType" Margin="10,0"/>
            <RadioButton x:Name="BackgroundRadioButton" Content="Background" GroupName="CharacterType" Margin="10,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="60" Margin="5,15,5,5" Click="Ok_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="60" Margin="5,15,5,5" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>